import { Metadata } from "next";

export const metadata: Metadata = {
  title: "RedTab",
};

export default function BusinessTypeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
