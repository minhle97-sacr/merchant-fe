import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Redtab Pay Later",
};

export default function RedtabPaylaterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
